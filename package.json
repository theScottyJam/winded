{
  "scripts": {
    "build": "winded --include 'docs/**/*.html' --output ./docs/style.css",
    "build:watch": "nodemon --watch './docs/**/*' -e html --exec 'npm run build'"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "winded": "^0.0.0"
  }
}
